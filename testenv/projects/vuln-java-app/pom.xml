<?xml version="1.0" encoding="UTF-8"?>
<!-- FORGEGUARDIAN TEST PROJECT — vulnerable Java app for patch agent testing -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.forgeguardian.test</groupId>
  <artifactId>vuln-java-app</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>
  <name>ForgeGuardian Test — Vulnerable Java App</name>

  <dependencies>
    <!-- CVE-2021-44228 — Log4Shell RCE (CVSS 10.0) -->
    <dependency>
      <groupId>org.apache.logging.log4j</groupId>
      <artifactId>log4j-core</artifactId>
      <version>2.14.1</version>
    </dependency>

    <!-- CVE-2015-6420 — Java deserialization RCE (CVSS 7.5) -->
    <dependency>
      <groupId>commons-collections</groupId>
      <artifactId>commons-collections</artifactId>
      <version>3.2.1</version>
    </dependency>

    <!-- CVE-2022-22965 — Spring4Shell RCE (CVSS 9.8) -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-core</artifactId>
      <version>5.3.17</version>
    </dependency>

    <!-- CVE-2021-26291 — Maven dependency confusion (CVSS 9.1) -->
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-core</artifactId>
      <version>3.6.3</version>
    </dependency>
  </dependencies>
</project>
