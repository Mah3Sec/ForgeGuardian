version: 2

project_name: forgeguardian

before:
  hooks:
    - go mod tidy

builds:
  - id: fgctl
    main: ./cmd/fgctl/
    binary: fgctl
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildTime={{.Date}}
      - -X github.com/mah3sec/forgeguardian/internal/license.publicKeyHex={{.Env.FG_LICENSE_PUBLIC_KEY}}

  - id: fg-agent
    main: ./cmd/fg-agent/
    binary: fg-agent
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildTime={{.Date}}
      - -X github.com/mah3sec/forgeguardian/internal/license.publicKeyHex={{.Env.FG_LICENSE_PUBLIC_KEY}}

  - id: intel-agent
    main: ./cmd/intel-agent/
    binary: intel-agent
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildTime={{.Date}}
      - -X github.com/mah3sec/forgeguardian/internal/license.publicKeyHex={{.Env.FG_LICENSE_PUBLIC_KEY}}

archives:
  - id: default
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch

brews:
  - name: forgeguardian
    ids:
      - default
    repository:
      owner: mah3sec
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/mah3sec/forgeguardian"
    description: "AI-powered supply chain security scanner for software packages"
    license: "Apache-2.0"
    install: |
      bin.install "fgctl"
      bin.install "fg-agent"
      bin.install "intel-agent"
    test: |
      assert_match version.to_s, shell_output("#{bin}/fgctl version")

release:
  github:
    owner: mah3sec
    name: forgeguardian
  draft: false
  prerelease: auto
  extra_files:
    - glob: dashboard-dist.tar.gz
  name_template: "ForgeGuardian {{.Version}}"
  header: |
    ## ForgeGuardian {{.Version}}

    ### Install (one script — auto-detects OS, installs everything)
    **Linux / macOS / Git Bash:**
    ```bash
    curl -sSfL https://raw.githubusercontent.com/Mah3Sec/ForgeGuardian/main/install.sh | bash
    ```
    **Windows PowerShell:**
    ```powershell
    irm https://raw.githubusercontent.com/Mah3Sec/ForgeGuardian/main/install.ps1 | iex
    ```
    Then start:
    ```bash
    fgctl serve    # → http://localhost:8080
    ```

    ### Docker
    ```bash
    docker run -d --name forgeguardian -p 3000:3000 ghcr.io/mah3sec/forgeguardian:{{.Tag}}
    ```

    Or download a prebuilt binary below.
