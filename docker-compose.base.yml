version: "3.9"

# Shared network and volume definitions — extended by profile-specific compose files.

networks:
  forgeguardian:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  minio_data:
  grafana_data:
  rekor_data:
